//e = user inputs the exercise the user did on a date
//v = usuer enters the specific date to view that days workout
//q = exit the program
Start
	output intialPrompt
	input userInput
		while userInput != q
			if userInput = e
				set e = Yes
				while e = Yes
					output excerisePrompt
					input date, name, reps, sets, weights
					output contiuneExcerisePrompt
					input userInput
					set e = userInput
					if e != Yes || No
						output wrongInput
						input userInput
				elsewhile e = No
					return
			elseif userInput = v
				set v = Yes
				while v = Yes
					output viewDay
					input date
					output exerciseDay
					output contiuneViewingPrompt
					input userInput
					set v = userInput
					if v != Yes || No
						output wrongInput
						input userInput
				elsewhile e = No
					return
			else
				output wrongEntery
				input userInput
		exit
Stop


//The Excersice class is going to prompt the user to enter a workout and hold the values the user input. Another class will retireve the information from this class
class Excersice
{
	public:
		string intialPrompt;
		char whatDo;
			//This is the choice the user makes. Either e, v, or q
		string enterExcersie;
		string continueAdding;
			//This is going to prompt the user if they wish to add additoinal workouts
		string wrongValue
			//This prompt will tell the user that they inputted a invaled character
	private:
		string name;
		//This is the name of the excersie
		int reps;
		int sets;
		int weights;
};

//This class will be used to retireve the values in the excersie class
class RetireveExercise
{
	public:
		string viewDay
			//This will prompt the user to enter a date they wish to view the particular workout
		
	private:
};

